# Optional override. The default is ../personal-content relative to this repo.
PERSONAL_CONTENT_DIR=../personal-content

# Store the age private identity in 1Password and resolve it only for commands
# that decrypt personal content.
SOPS_AGE_KEY="op://Developer/Personal Content age identity/notesPlain"
